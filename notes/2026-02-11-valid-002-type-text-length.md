# 2026-02-11 VALID-002 type text length validation

- Validator enforces `max_text_len` (default 2000) using character count.
- Added boundary unit coverage for max length accepted and max+1 rejected.
