# 2026-02-11 CDP-004 Click action

- Click now resolves snapshot element ids via the element map and executes a CDP click at the backend node's box-model center.
- `CdpBrowser::apply` returns a structured `StepResult` with `ok` true on success and a typed `StepError` on failure.
- E2E harness validates click success by asserting visible text changes after applying a click action.
