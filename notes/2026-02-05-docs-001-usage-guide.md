# 2026-02-05 DOCS-001 usage + ops guide

- Added `README.md` as the primary usage and operations guide.
- Documented install prerequisites, CLI usage, config precedence, and a sample
  `mbus.toml` aligned with `src/config.rs` defaults.
- Included env var override list and scripted actions format.
- Added troubleshooting tips plus a verification/rollback runbook.
