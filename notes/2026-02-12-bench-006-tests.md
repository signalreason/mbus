# 2026-02-12 BENCH-006 benchmark tests

- Added unit coverage for `LlmMode` parsing (case/whitespace, unknown value).
- Added aggregate usage + cost edge-case tests (missing usage, invalid pricing, missing tokens, zero tokens).
- Added integration tests for scripted action files and mocked OpenAI usage aggregation using a fake browser.
