{
  "id": "challenge-09-accordion",
  "task": "Open the return policy accordion so the page shows RETURNS OPEN.",
  "start_url": "{{base_url}}/challenge/accordion.html",
  "allowed_domains": ["127.0.0.1"],
  "max_steps": 20,
  "expect": {
    "final_url_contains": "/challenge/accordion.html",
    "final_visible_text_contains": "RETURNS OPEN",
    "screenshot_artifact_required": true
  }
}
