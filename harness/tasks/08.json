{
  "id": "bench-task-08",
  "task": "Navigate to benchmark task 08 and confirm marker text.",
  "start_path": "/bench/start",
  "max_steps": 40,
  "actions": [
    {"type": "navigate", "url": "{{base_url}}/bench/task-08"},
    {"type": "done", "summary": "Reached benchmark task 08"}
  ],
  "expect": {
    "status": "done",
    "final_url_contains": "/bench/task-08",
    "final_visible_text_contains": "BENCH TASK 08"
  }
}
